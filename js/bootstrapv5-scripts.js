var myCarousel = document.getElementById("carouselExampleDark");
var carousel = new bootstrap.Carousel(myCarousel, {
  interval: 5000, // Cambia este valor para ajustar la velocidad del autoplay
  wrap: true, // Cambia a 'false' si no quieres que el carrusel se reinicie al llegar al final
});
var myCarousel = document.getElementById("carouselExampleDark1");
var carousel = new bootstrap.Carousel(myCarousel, {
  interval: 5000, // Cambia este valor para ajustar la velocidad del autoplay
  wrap: true, // Cambia a 'false' si no quieres que el carrusel se reinicie al llegar al final
});
